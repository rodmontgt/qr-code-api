version: '3.8'

services:
  qr-api:
    build: .
    container_name: qr-code-api
    ports:
      - "8080:80"
    environment:
      - API_USERNAME=${API_USERNAME:-admin}
      - API_PASSWORD=${API_PASSWORD:-changeme}
    restart: unless-stopped